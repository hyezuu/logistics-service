spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: auth
  profiles:
    active: local
  cloud:
    config:
      uri: http://config:8888
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://server:18999/eureka/
